#include <cmath>
#include <ios>
#include <iostream>
#include <vector>

using ll = int64_t;

int main(void) {
	std::ios::sync_with_stdio(0);
	std::cin.tie(0);
	std::cout.tie(0);
	int n, k;
	std::cin >> n >> k;

	return 0;
}
